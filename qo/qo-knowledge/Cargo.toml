[package]
name = "qo-knowledge"
version.workspace = true
edition.workspace = true
description = "QO knowledge graph — claims with provenance, evidence and revisions"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
redb = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
# Ed25519 for delta signatures — the audited implementation, not a local one.
qlang-core = { workspace = true }

[dev-dependencies]
tempfile = "3"
